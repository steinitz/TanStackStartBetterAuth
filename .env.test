# Test environment variables for Playwright E2E tests
# Using PLAYWRIGHT_TEST_ENV instead of NODE_ENV to avoid React SWC issues
PLAYWRIGHT_TEST_ENV=test

# Add other test-specific environment variables here as needed
# BASE_URL=http://localhost:3000
# TEST_EMAIL=test@example.com